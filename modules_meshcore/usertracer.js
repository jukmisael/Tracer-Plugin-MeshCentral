/**
 * User-Device Tracer — Agent-side module
 * Minimal placeholder — user detection is now done server-side
 * using MeshCentral's built-in agent user data (device.users/lusers).
 */
"use strict";

function consoleaction(args, rights, sessionid, parent) {
    return 'OK';
}
